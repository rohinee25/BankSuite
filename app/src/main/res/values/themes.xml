<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BankSuite" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorPrimary">@color/bank_primary</item>
        <item name="android:colorPrimaryDark">@color/bank_primary_dark</item>
        <item name="android:colorAccent">@color/bank_accent</item>
        <item name="android:windowBackground">@color/gray_100</item>
        <item name="android:statusBarColor">@color/bank_primary</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.BankSuite.Splash" parent="Theme.BankSuite">
        <item name="android:windowBackground">@drawable/ic_splash</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>